{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Launch Chorome against localhost",
            "url": "http://localhost:3006",
            "webRoot": "${workspaceFolder}"
        }
    ]
}